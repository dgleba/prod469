
<%= form_for(@elist_report, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @elist_report.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@elist_report.errors.count, "error") %> prohibited this elist_report from being saved:</h4>
      <ul>
      <% @elist_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
            <%= f.label :when_to_send, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.text_field :when_to_send, class: "form-control" %>
      </div>
        </div>
  <div class="form-group">
            <%= f.label :elist_id, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.collection_select :elist_id, Elist.all, :id, :name,  { prompt: true }, { :class => 'dgselect2' }  %>
        </div>
    </div>
  <div class="form-group">
            <%= f.label :report_id, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.collection_select :report_id, Report.all, :id, :name,  { prompt: true }, { :class => 'dgselect2' }  %>
        </div>
    </div>
  <div class="form-group">
            <%= f.label :active_status, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.number_field :active_status, class: "form-control" %>
      </div>
        </div>
  <div class="form-group">
            <%= f.label :sort_order, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.number_field :sort_order, class: "form-control" %>
      </div>
        </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
