
<row class="row is-table-row" >

  <div class="col-sm-1 ">
    <%= link_to 'Create', new_elist_path , class: "btn btn-success"%>
  </div>

  <div class="col-sm-2 ">
    <h4>Listing Elists</h4>
  </div>

  <div class="col-sm-3">
    <%= search_form_for @q, class: 'search-form' do |f| %>
      <div class="input-group">
        <%= f.search_field :email_cont, class: 'form-control' %>
        <span class="input-group-btn">
          <button class="btn btn-primary">Search <i class="fa fa-search"></i>
          </button>
        </span>
      </div>
    <% end %>
  </div>

  <div class="col-sm-4 ">
    <%= paginate @elists %>
  </div>
  
  <div class="col-sm-2 ">
    <%= page_entries_info @elists, :entry_name => 'item' %>
  </div>
  
</row>


<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="1"></th>
            <th>Email</th>
      <th>Active status</th>
      <th>Sort order</th>
    </tr>
  </thead>

  <tbody>
    <% @elists.each do |elist| %>
      <tr>
        <td>
          <div class="btn-group">
          <%= link_to 'Show', elist, class: "btn btn-info", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Edit', edit_elist_path(elist), class: "btn btn-primary", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Destroy', elist, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning", :style=>'color:#FFFFFF;' %>
          </div>
        </td>
        
        <td><%= elist.email %></td>
        <td><%= elist.active_status %></td>
        <td><%= elist.sort_order %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
